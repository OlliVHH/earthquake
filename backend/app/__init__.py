"""Earthquake analytics backend application."""

# Human: Package marker for `app`; import submodules (main, config, models) explicitly from callers.
# Agent: No side effects at import; READS nothing; used as Python package root for backend services.
